<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.happyjob.jobfolio.repository.notice.BoardInfoRepository">
	
   	<!-- 이용안내 목록 조회 -->
	<select id = "selectByBoardType" parameterType="String" resultType = "com.happyjob.jobfolio.vo.notice.BoardInfoVo">
        
		SELECT
			board_no AS id,
			title AS question,
			content AS answer,
			write_date AS createdAt,
			author AS writer,
			priority
		FROM tb_board
		WHERE board_type = #{board_type}
		ORDER BY priority DESC
	</select>
	
	<!-- 이용안내 등록 -->
	<insert id="insertBoardInfo" parameterType="com.happyjob.jobfolio.vo.notice.BoardInfoVo">
		
		<selectKey keyProperty="id" resultType="int" order="BEFORE">
			SELECT IFNULL(MAX(board_no), 0) + 1 FROM tb_board
		</selectKey>
		
		INSERT INTO tb_board (
			board_no,
			board_type,
            title,
            content,
            write_date,
            author,
            priority
            
		) VALUES (
			#{id},
			#{board_type},
			#{question},
			#{answer},
			now(),
			1,
			#{priority}
		)
	</insert>
	
	<!-- Java에서 호출해서 priority값을 구하는 쿼리 -->
	<select id="getNextPriority" resultType="int">
		SELECT IFNULL(MAX(priority), 0) + 1 FROM tb_board
	</select>
	
</mapper>