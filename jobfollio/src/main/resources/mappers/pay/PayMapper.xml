<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.happyjob.jobfolio.repository.pay.PayMapper">

	<!-- 결제 DB 저장 -->
	<insert id="insertOrder" parameterType="map">
		INSERT INTO 	tb_payment_his
		(
				order_Id
		,		product_no
		,		order_name
		,		amount
		,		user_no
		,		pay_status
		,		paid_date
		) VALUES (
						#{orderId}
				 ,		#{product_no}
				 ,		#{order_name}
				 ,		#{amount}
				 ,		#{user_no}
				 ,		'1'
				 ,		NOW()
				 )
	</insert>

	<!-- 결제 DB 저장 -->
	<update id="cardSuccess" parameterType="com.happyjob.jobfolio.vo.pay.PayModel">
		UPDATE 		tb_payment_his
		SET 		payment_key		 = 	#{paymentKey}
			  ,		pay_status 		 = 	'2'
			  ,  	paid_date 	     =  NOW()
		WHERE 		order_Id	 	 =  #{orderId}
	</update>

</mapper>